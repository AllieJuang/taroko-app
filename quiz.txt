I prefer to code like Code 2. Beacause Code 2 is more specific and more flexible.
If using JavaScript, binding click event on parent dom may makes browser's EventListeners less(Event Delegation). 
But React's event is not binded on native Document Dom.
